{
  "name": "company-research-agent-root",
  "version": "1.0.0",
  "description": "Multi-Agent Company Research Tool using React, Node.js, and LangGraph",
  "scripts": {
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
